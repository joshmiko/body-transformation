{
  "name": "body-transformation",
  "version": "0.1.0",
  "private": true,
  "description": "Data-first personal fitness tracker",
  "scripts": {
    "verify": "node scripts/verify-static-app.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run verify && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
